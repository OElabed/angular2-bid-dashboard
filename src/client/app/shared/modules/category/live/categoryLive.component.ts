import { Component, Input } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'category-live-cmp',
    templateUrl: 'category-live.component.html',
})

export class CategoryLiveComponent { }
