import { Route } from '@angular/router';
import { LiveAuctionsComponent } from './index';

export const LiveAuctionsRoutes: Route[] = [
    {
        path: 'live-auctions',
        component: LiveAuctionsComponent
    }
];
